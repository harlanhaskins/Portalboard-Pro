function TurretAssistant(argFromPusher) {}

TurretAssistant.prototype = {
	setup: function() {
		Ares.setupSceneAssistant(this);
		var audio1 = new Audio();
	},
	cleanup: function() {
		Ares.cleanupSceneAssistant(this);
	},
	button1Tap: function(inSender, event) {
		url = 'audio/t_could_you_come_over_here.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button2Tap: function(inSender, event) {
		url = 'audio/t_hello_friend.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button3Tap: function(inSender, event) {
		url = 'audio/t_hey_itzzzs_me.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button4Tap: function(inSender, event) {
		url = 'audio/t_i_donzzzt_understand.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button5Tap: function(inSender, event) {
		url = 'audio/t_is_anyone_there.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button6Tap: function(inSender, event) {
		url = 'audio/t_it_burns.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button7Tap: function(inSender, event) {
		url = 'audio/t_it_wont_be_enough.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button8Tap: function(inSender, event) {
		url = 'audio/t_izzzm_on_fire.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button9Tap: function(inSender, event) {
		url = 'audio/t_my_fault.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button10Tap: function(inSender, event) {
		url = 'audio/t_naptime.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button11Tap: function(inSender, event) {
		url = 'audio/t_sorry.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button12Tap: function(inSender, event) {
		url = 'audio/t_target_aquired.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button13Tap: function(inSender, event) {
		url = 'audio/t_who_are_you.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	picTap: function(inSender, event) {
		this.controller.stageController.popScene('Turret');
		this.controller.stageController.pushScene('main');
	}
};