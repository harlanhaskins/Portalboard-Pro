function MainAssistant(argFromPusher) {
}

MainAssistant.prototype = {
	setup: function() {
		Ares.setupSceneAssistant(this);
	},
	cleanup: function() {
		Ares.cleanupSceneAssistant(this);
	},
	button1Tap: function(inSender, event) {
		this.controller.stageController.pushScene("GLaDOS");
	},
	button2Tap: function(inSender, event) {
		this.controller.stageController.pushScene("CaveJohnson");
	},
	button3Tap: function(inSender, event) {
		this.controller.stageController.pushScene("Wheatley");
	},
	button4Tap: function(inSender, event) {
		this.controller.stageController.pushScene("Turret");
	},
	button5Tap: function(inSender, event) {
		this.controller.stageController.pushScene("DefectiveTurret");
	},
	button6Tap: function(inSender, event) {
		this.controller.stageController.pushScene("SpaceCore");
	},
	button8Tap: function(inSender, event) {
		this.controller.stageController.pushScene("AdventureCore");
	},
	button9Tap: function(inSender, event) {
		this.controller.stageController.pushScene("FactCore");
	},
	button7Tap: function(inSender, event) {
		window.location = "http://www.twitter.com/harlanhaskins";
	},
	donateTap: function(inSender, event){
		window.location = "http://dft.ba/-donatetoharlan";
	}
};