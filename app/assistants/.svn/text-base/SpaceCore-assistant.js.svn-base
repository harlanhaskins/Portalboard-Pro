function SpaceCoreAssistant(argFromPusher) {}

SpaceCoreAssistant.prototype = {
	setup: function() {
		Ares.setupSceneAssistant(this);
		var audio1 = new Audio();
	},
	cleanup: function() {
		Ares.cleanupSceneAssistant(this);
	},
	button1Tap: function(inSender, event) {
		url = 'audio/s_bam_bam_take_that_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button2Tap: function(inSender, event) {
		url = 'audio/s_bored_of_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button3Tap: function(inSender, event) {
		url = 'audio/s_dad_im_in_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button4Tap: function(inSender, event) {
		url = 'audio/s_dont_like_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button5Tap: function(inSender, event) {
		url = 'audio/s_going_to_space_canzzzt_wait.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button6Tap: function(inSender, event) {
		url = 'audio/s_grandma_yes_izzzm_in_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button7Tap: function(inSender, event) {
		url = 'audio/s_hehehe_im_in_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button8Tap: function(inSender, event) {
		url = 'audio/s_space_space.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button9Tap: function(inSender, event) {
		url = 'audio/s_space_yell.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
	button10Tap: function(inSender, event) {
		url = 'audio/s_therzzz_a_star.mp3';
		audio1.src = url;
		audio1.load();
		audio1.play();
	},
		picTap: function(inSender, event) {
		this.controller.stageController.popScene('SpaceCore');
		this.controller.stageController.pushScene('main');
	}
};